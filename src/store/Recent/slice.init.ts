import { RecentState } from './slice.interface';

const initialState: RecentState = {
  list: [
    'react',
    'angular',
    'mongodb',
    'postgres',
    'node',
  ],
};

export {
  initialState,
}
